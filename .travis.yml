language: cpp

compiler:
  - gcc

install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install binutils-mingw-w64 gcc-mingw-w64 g++-mingw-w64

script:
  - cd ./src
  - make mingw32_defconfig
  - make
  - ls -la
